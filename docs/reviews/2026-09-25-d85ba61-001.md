# Security review d85ba61 (iteration 001)

- Reviewed commit: d85ba6168f84c10a971455c4e90400b195543ec3
- Base: 2bd4dfeeda2c43b2739bbab738b051d77395c550
- Range: 2bd4dfeeda2c43b2739bbab738b051d77395c550..HEAD
- Iteration: 001
- Date: 2026-09-25
- Verdict: PASS

## Summary

The range has two commits. 70342c9 updates the docs after the first push: CLAUDE.md, docs/operations.md, the README's backup commands and a compose comment. d85ba61 hardens `db/backup.sh`, adds zstd compression and a decisions.md entry. I found nothing to report in any category. The backup change fixes a real integrity bug: a failed `pg_dump` inside `backup_once || ...` used to be renamed as a finished dump and still pruned older backups. Every step now returns on failure, so pruning (`find ... -delete`, db/backup.sh:29) runs only after a dump that has been written and checked with `pg_restore --list`. The secrets scan is empty and pip-audit reports no known vulnerabilities. No dependencies or images change. Nothing needs to happen before the push.

## Findings

No findings.

Notes for the record (not findings):
- db/backup.sh:21, `stamp=$(date ...)` is not checked. In the loop, where `set -e` does not apply, a failing `date` would leave the stamp empty. This has no security impact, and the `pg_dump` and `pg_restore --list` checks still hold.
- `--compress=zstd:19` needs pg_dump 16 or later. The backup service uses `postgres:17-alpine` (compose.yaml:60), and the owner tested a zstd dump and restore on the live instance.
- The new docs mention only `~/.collegium/...` with a tilde, so no absolute home path. The `set -a; . ~/.collegium/.env; set +a` step in docs/operations.md:145 loads the env into a command's environment without printing it, which matches the project's rule for secrets.
- The commit identity `Ketil Aasarød <ketil.aasarod@gmail.com>` is the same one already published in the first push. No employer address appears.

## Earlier findings

This is iteration 001 of a new push loop, so there are no earlier findings in this loop. The findings deferred by the owner in earlier reviews (SEC-8 to SEC-12, SEC-14, SEC-16) are unchanged by this range. The only change is that CLAUDE.md's open-threads list now names all of them as follow-up work to propose.

## Not checked

- The history scan for the employer's name and domain, home paths, the earlier project's name, the third party's name, the host name and Zscaler was run by the main session. I could not run it myself, and I rely on its report of no matches. The six changed files in diff.patch contain none of these terms.
- The live behaviour of the backup script (0-byte dump before the change, no dump and a logged failure after it, zstd restore with matching row counts) was tested by the main session, not by me.
- File permissions on the backup files in `~/.collegium/backups` on the host are unchanged by this range and were not examined.
